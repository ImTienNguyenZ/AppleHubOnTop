function TienTiny:AddSeperator(name)
    TienTiny:AddParagraph({Title = name,Content = ""})
end
function Settings:AddSeperator(name)
    Settings:AddParagraph({Title = name,Content = ""})
end
function Main:AddSeperator(name)
    Main:AddParagraph({Title = name,Content = ""})
end
function M:AddSeperator(name)
    M:AddParagraph({Title = name,Content = ""})
end
function Ss:AddSeperator(name)
    Ss:AddParagraph({Title = name,Content = ""})
end
function RaceV4:AddSeperator(name)
    RaceV4:AddParagraph({Title = name,Content = ""})
end
function EventGame:AddSeperator(name)
    EventGame:AddParagraph({Title = name,Content = ""})
end
function P:AddSeperator(name)
    P:AddParagraph({Title = name,Content = ""})
end
function Status:AddSeperator(name)
    Status:AddParagraph({Title = name,Content = ""})
end
function Visual:AddSeperator(name)
    Visual:AddParagraph({Title = name,Content = ""})
end
function R:AddSeperator(name)
    R:AddParagraph({Title = name,Content = ""})
end
function T:AddSeperator(name)
    T:AddParagraph({Title = name,Content = ""})
end
function S:AddSeperator(name)
    S:AddParagraph({Title = name,Content = ""})
end
function D:AddSeperator(name)
    D:AddParagraph({Title = name,Content = ""})
end
function Misc:AddSeperator(name)
    Misc:AddParagraph({Title = name,Content = ""})
end
function TienTiny:AddLabel(name)
    return TienTiny:AddParagraph({Title = name, Content = ""})
end

function Settings:AddLabel(name)
    return Settings:AddParagraph({Title = name, Content = ""})
end

function Main:AddLabel(name)
    return Main:AddParagraph({Title = name, Content = ""})
end

function M:AddLabel(name)
    return M:AddParagraph({Title = name, Content = ""})
end

function Ss:AddLabel(name)
    return Ss:AddParagraph({Title = name, Content = ""})
end

function RaceV4:AddLabel(name)
    return RaceV4:AddParagraph({Title = name, Content = ""})
end

function EventGame:AddLabel(name)
    return EventGame:AddParagraph({Title = name, Content = ""})
end

function P:AddLabel(name)
    return P:AddParagraph({Title = name, Content = ""})
end

function Status:AddLabel(name)
    return Status:AddParagraph({Title = name, Content = ""})
end

function Visual:AddLabel(name)
    return Visual:AddParagraph({Title = name, Content = ""})
end

function R:AddLabel(name)
    return R:AddParagraph({Title = name, Content = ""})
end

function T:AddLabel(name)
    return T:AddParagraph({Title = name, Content = ""})
end

function S:AddLabel(name)
    return S:AddParagraph({Title = name, Content = ""})
end

function D:AddLabel(name)
    return D:AddParagraph({Title = name, Content = ""})
end

function Misc:AddLabel(name)
    return Misc:AddParagraph({Title = name, Content = ""})
end
function TienTiny:Button(name, callback)
    return TienTiny:AddButton({Title = name, Content = "", Callback = callback})
end

function Settings:Button(name, callback)
    return Settings:AddButton({Title = name, Content = "", Callback = callback})
end

function Main:Button(name, callback)
    return Main:AddButton({Title = name, Content = "", Callback = callback})
end

function M:Button(name, callback)
    return M:AddButton({Title = name, Content = "", Callback = callback})
end

function Ss:Button(name, callback)
    return Ss:AddButton({Title = name, Content = "", Callback = callback})
end

function RaceV4:Button(name, callback)
    return RaceV4:AddButton({Title = name, Content = "", Callback = callback})
end

function EventGame:Button(name, callback)
    return EventGame:AddButton({Title = name, Content = "", Callback = callback})
end

function P:Button(name, callback)
    return P:AddButton({Title = name, Content = "", Callback = callback})
end

function Status:Button(name, callback)
    return Status:AddButton({Title = name, Content = "", Callback = callback})
end

function Visual:Button(name, callback)
    return Visual:AddButton({Title = name, Content = "", Callback = callback})
end

function R:Button(name, callback)
    return R:AddButton({Title = name, Content = "", Callback = callback})
end

function T:Button(name, callback)
    return T:AddButton({Title = name, Content = "", Callback = callback})
end

function S:Button(name, callback)
    return S:AddButton({Title = name, Content = "", Callback = callback})
end

function D:Button(name, callback)
    return D:AddButton({Title = name, Content = "", Callback = callback})
end

function Misc:Button(name, callback)
    return Misc:AddButton({Title = name, Content = "", Callback = callback})
end
function TienTiny:Toggle(name, def, callback)
    return TienTiny:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function Settings:Toggle(name, def, callback)
    return Settings:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function Main:Toggle(name, def, callback)
    return Main:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function M:Toggle(name, def, callback)
    return M:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function Ss:Toggle(name, def, callback)
    return Ss:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function RaceV4:Toggle(name, def, callback)
    return RaceV4:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function EventGame:Toggle(name, def, callback)
    return EventGame:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function P:Toggle(name, def, callback)
    return P:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function Status:Toggle(name, def, callback)
    return Status:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function Visual:Toggle(name, def, callback)
    return Visual:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function R:Toggle(name, def, callback)
    return R:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function T:Toggle(name, def, callback)
    return T:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function S:Toggle(name, def, callback)
    return S:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function D:Toggle(name, def, callback)
    return D:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end

function Misc:Toggle(name, def, callback)
    return Misc:AddToggle(name,{Title = name, Default  = def,Content = "", Callback = callback})
end
function TienTiny:Dropdown(name, values, callback)
    return TienTiny:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function Settings:Dropdown(name, values, callback)
    return Settings:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function Main:Dropdown(name, values, callback)
    return Main:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function M:Dropdown(name, values, callback)
    return M:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function Ss:Dropdown(name, values, callback)
    return Ss:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function RaceV4:Dropdown(name, values, callback)
    return RaceV4:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function EventGame:Dropdown(name, values, callback)
    return EventGame:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function P:Dropdown(name, values, callback)
    return P:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function Status:Dropdown(name, values, callback)
    return Status:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function Visual:Dropdown(name, values, callback)
    return Visual:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function R:Dropdown(name, values, callback)
    return R:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function T:Dropdown(name, values, callback)
    return T:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function S:Dropdown(name, values, callback)
    return S:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function D:Dropdown(name, values, callback)
    return D:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function Misc:Dropdown(name, values, callback)
    return Misc:AddDropdown(name, {
        Title = name,
        Values = values,
        Multi = false,
        Default = 1,
        Callback = callback
    })
end

function TienTiny:TextBox(name, def, callback)
    return TienTiny:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function Settings:TextBox(name, def, callback)
    return Settings:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function Main:TextBox(name, def, callback)
    return Main:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function M:TextBox(name, def, callback)
    return M:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function Ss:TextBox(name, def, callback)
    return Ss:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function RaceV4:TextBox(name, def, callback)
    return RaceV4:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function EventGame:TextBox(name, def, callback)
    return EventGame:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function P:TextBox(name, def, callback)
    return P:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function Status:TextBox(name, def, callback)
    return Status:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function Visual:TextBox(name, def, callback)
    return Visual:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function R:TextBox(name, def, callback)
    return R:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function T:TextBox(name, def, callback)
    return T:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function S:TextBox(name, def, callback)
    return S:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function D:TextBox(name, def, callback)
    return D:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function Misc:TextBox(name, def, callback)
    return Misc:AddInput(name, {
        Title = name,
        Default =  "",
        Placeholder = "Enter You Text..",
        Numeric = def,
        Finished = def,
        Callback = callback
    })
end

function TienTiny:Slider(name, min, max, values, callback)
    return TienTiny:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end

function Settings:Slider(name, min, max, values, callback)
    return Settings:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end

function Main:Slider(name, min, max, values, callback)
    return Main:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end

function M:Slider(name, min, max, values, callback)
    return M:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end

function Ss:Slider(name, min, max, values, callback)
    return Ss:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end

function RaceV4:Slider(name, min, max, values, callback)
    return RaceV4:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end
function Settings:Slider(name, min, max, values, callback)
    return Settings:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end
--
function EventGame:Slider(name, min, max, values, callback)
    return EventGame:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end

function P:Slider(name, min, max, values, callback)
    return P:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end

function Status:Slider(name, min, max, values, callback)
    return Status:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end

function Visual:Slider(name, min, max, values, callback)
    return Visual:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end
function R:Slider(name, min, max, values, callback)
    return R:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end
function T:Slider(name, min, max, values, callback)
    return T:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end
function S:Slider(name, min, max, values, callback)
    return S:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end
function D:Slider(name, min, max, values, callback)
    return D:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end
function Misc:Slider(name, min, max, values, callback)
    return Misc:AddSlider(name, {
        Title = name,
        Description = "",
        Default = values,
        Min = min,
        Max = max,
        Rounding = 1,
        Callback = callback
    })
end
